spring:
  application:
    name: HystrixDashboard
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
